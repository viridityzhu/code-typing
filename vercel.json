{
    "builds": [{
        "src": "codeTyping/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "codeTyping/wsgi.py"
        }
    ]
}
